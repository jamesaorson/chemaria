function init(self)
	msg.post("#worldproxy", "load")
end

function on_message(self, messageId, message, sender)
	if messageId == hash("proxy_loaded") then
		msg.post(sender, "init")
		msg.post(sender, "enable")
		msg.post(sender, "acquire_input_focus")
	end
end
